import { BorderIconDirective } from './border-icon.directive';

describe('BorderIconDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderIconDirective();
    expect(directive).toBeTruthy();
  });
});
